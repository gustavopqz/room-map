//Event listener in modality
const modality = document.getElementById('modality')

modality.addEventListener('change', ()=>{
    if(modality.value == 'pós'){
        if(window.innerWidth > 680){
            
        }
    }
})